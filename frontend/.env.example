# Leave unset to use Vite's dev proxy (/api -> http://localhost:8000, see vite.config.ts).
# Set to hit a deployed backend directly instead (e.g. the AWS ALB URL) -
# requires the backend's CORS allow_origins to include this frontend's origin.
VITE_API_BASE_URL=
